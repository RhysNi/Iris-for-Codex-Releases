# Iris for Codex 0.1.7

## 新增功能

- 新增“自动 / 菜单栏图标 / 刘海屏”三种状态栏样式；自动模式会根据当前屏幕是否具有实体刘海选择合适的呈现方式。
- 新增 Iris 专用菜单栏模板图标，并使用 macOS 系统分配的菜单栏槽位，支持浅色、深色和彩色菜单栏背景。
- 支持向能够唯一验证归属且正在运行的 Codex CLI 主会话安全加入文字或端侧语音转写消息；Codex Desktop 与 CLI 复用统一的等待、失败和重试交互。
- 语音输入改为默认关闭的可选端侧功能；启用后支持输入框内状态提示和随主题变化的焦点跑马灯。
- 设置页按通用、主题、语音输入、授权规则和软件更新重新分类，并统一使用原生 macOS 控件。

## 修复项

- 修复无刘海屏幕上 Iris 刘海灯条可能遮挡其他应用菜单栏图标的问题。
- 修复专用菜单栏图标上下颠倒、完整应用图标缩小时视觉过小且细节难以辨认的问题。
- 修复会话发现短暂空窗时任务列表瞬间清空的问题；失去实时归属证据后仍会立即撤销消息发送和跳转能力。
- 修复语音状态文案可能在屏幕底部被裁切，以及点击语音按钮意外获取输入焦点的问题。
- 修复设置页分类、开关样式、更新控件对齐和连接配置残留等界面问题。
