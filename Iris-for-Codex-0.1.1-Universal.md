# Iris for Codex 0.1.1

## 新增功能

- 支持向唯一归属、经过验证的 Codex Desktop 主会话发送文字，并可将按住说话的转写结果编辑后发送。
- 最近对话支持 Markdown、代码高亮、可点击链接、横向滚动表格、图片缩略图与大图查看。
- 兼容 Codex CLI 0.155.1 的五类受信 Hook，并按结构化 turn 展示最近十轮对话。
- 设置页新增在线更新与对话 Markdown 控制项，正式更新包继续经过 Sparkle EdDSA、Developer ID 和 Apple 公证校验。

## 修复项

- 修复 Codex Desktop v2 IPC 请求结构错误导致消息无法发送的问题。
- 清理图片附件注入的内部文件清单、临时路径和边界提示，不再把协议元数据展示成用户正文。
- 隐藏内部 memory citation 元数据，避免其出现在对话内容中。
- 隐藏尚未绑定真实 rollout 的生命周期 Hook 占位会话，不再生成独立的 “Codex Hook” 分组。
- 修复用户消息宽度、对话边距、卡片间距、滚动条和表格布局等显示问题。
- 修复生命周期回调与官方用量刷新不同步的问题，同时保持会话输入、跳转和授权处理的 fail-closed 边界。
